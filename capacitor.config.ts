import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.exnet.cloud',
  appName: 'exnetcloud',
  webDir: 'dist',
  bundledWebRuntime: false
}

export default config
